export interface cardTypes {
  atk: string;
  def: string;
  name: string;
  card_images: { image_url: string }[];
  type: string;
  level: number;
  attribute: string;
  desc: string;
  race: string;
}
